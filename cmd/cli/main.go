// Command wisp is the CLI/TUI: one-shot subcommands, or an interactive
// terminal UI when run with no subcommand.
package main

import "fmt"

func main() {
	fmt.Println("wisp CLI - not yet implemented")
}
